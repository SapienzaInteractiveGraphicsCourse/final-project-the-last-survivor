export const canvas = document.querySelector(
    "#renderCanvas"
);

export const ammo = document.querySelector(
    "#ammo"
);

export const divFps = document.querySelector(
    "#fps"
);

export const runData = document.querySelector(
    "#runData"
);

export const luckyBox = document.getElementById(
    "luckyBox"
);
